/** JSK OS Build 1010 - Quote Management UI. */
function renderQuoteUi(){ensureBuild1010Quotes();var t=HtmlService.createTemplateFromFile('Ui/Quote/Quote');t.applicationName=JSKOS.Config.APP.NAME;t.applicationVersion=JSKOS.Config.APP.VERSION;t.currentUser=JSKOS.ConfigService.getCurrentUser();t.routeUrls=JSKOS.Router.getRouteUrls();var html=t.evaluate().getContent().replace('</body>',include('Ui/Quote/QuotePhase4Scripts')+'</body>');return HtmlService.createHtmlOutput(html).setTitle('Quotes | JSK OS').setXFrameOptionsMode(HtmlService.XFrameOptionsMode.ALLOWALL).addMetaTag('viewport','width=device-width, initial-scale=1');}
