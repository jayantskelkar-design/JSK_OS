/** JSK OS Build 1009 - Endorsement Management UI. */
function renderEndorsementUi(){ensureBuild1009Endorsements();var t=HtmlService.createTemplateFromFile('Ui/Endorsement/Endorsement');t.applicationName=JSKOS.Config.APP.NAME;t.applicationVersion=JSKOS.Config.APP.VERSION;t.currentUser=JSKOS.ConfigService.getCurrentUser();t.routeUrls=JSKOS.Router.getRouteUrls();return t.evaluate().setTitle('Endorsements | JSK OS').setXFrameOptionsMode(HtmlService.XFrameOptionsMode.ALLOWALL).addMetaTag('viewport','width=device-width, initial-scale=1');}
function testEndorsementUiRendering(){var c=renderEndorsementUi().getContent();['Endorsement Management','endorsementTableBody','apiEndorsementSearch'].forEach(function(m){if(c.indexOf(m)===-1)throw new Error('Missing Endorsement UI marker: '+m);});return{success:true,contentLength:c.length};}
